# 安装教程

## 手机 / 平板：安装到主屏幕

1. 用 Chrome 或 Safari 打开项目的 HTTPS 地址。
2. 首次进入会先显示“安装到当前设备”。Android 点击“安装”；没有安装按钮时打开浏览器菜单，选择“安装应用”。
3. iPhone / iPad 打开浏览器的分享菜单，选择“添加到主屏幕”；当前浏览器没有该入口时再改用 Safari。也可以先点“继续设置主持方式”试用网页版。
4. 选择“桌上有实体魔典”或“没有实体魔典”，再点击“开始配板”。以后会沿用这次选择，可随时更改。
5. 第一次完整打开后，核心主持功能可以离线使用；真实 AI 和云端归档仍需联网。安装入口之后仍可在“应用设置 → 安装与离线使用”找到。

对局默认只存在当前浏览器。换设备、清浏览器数据或重装前，打开“应用设置 → 导出与恢复当前对局”，先导出 JSON；在新设备选择文件，核对板子、人数和进度后，再确认恢复。

## Windows：便携包

### 1. 下载

打开 [GitHub Releases](https://github.com/Olalall/AI-The-Storyteller-of-the-Blood-Stained-Clock-Tower/releases)，下载：

`botc-storyteller-companion-windows-portable.zip`

> 不要下载 **Code → Download ZIP**，那是源码，不是便携包。

### 2. 解压

右键 ZIP → **全部解压**。不要在压缩包里直接运行。

### 3. 启动

双击解压目录里的：

`启动血染钟楼AI说书人工具.cmd`

浏览器会自动打开 `http://127.0.0.1:8787`。黑色启动窗口需要保持开启，关闭窗口即停止应用。

### 4. 首次设置

- **角色素材**：建议输入 `Y` 安装，共 718 个图标，约 102 MB。
- **AI 配置**：可填写接入地址、模型名和 API Key，也可以先跳过。
- 如果跳过素材，之后双击 `安装角色素材.cmd` 即可补装。

### API Key 保存在哪里

- 在网页“应用设置”中点击保存后，地址、模型和 API Key 会保存在当前设备、当前浏览器、当前站点的 `localStorage`，以后打开同一地址会继续使用。
- 这项保存不会把 Key 上传到 GitHub，也不会把它保存成 VPS/服务器配置；换设备、换浏览器或清除站点数据后需要重新填写。
- Windows 便携包启动器首次填写的后端 AI 配置保存在解压目录的本机 `.env`，与网页里的浏览器保存是两套位置，都不会随仓库发布。
- 公共电脑或多人共用浏览器不要保存 Key；需要删除时，在设置中恢复默认，或清除该站点数据。

## 常见问题

- **Windows 拦截脚本**：选择“更多信息”→“仍要运行”。
- **提示缺少运行环境**：通常是下载了源码 ZIP，或没有完整解压；请重新下载便携包。
- **必须运行源码 ZIP**：需要 Node.js `^20.19.0` 或 `>=22.12.0`；便携包已自带兼容版本。
- **AI 不可用**：不影响夜序、记录、投票、日志和归档。

更完整的说明见 [Windows 零安装便捷包说明](docs/QUICK_START_WINDOWS.md)。便携包内对应文件名为 `完整安装说明.md`。
